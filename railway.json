{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "(cd frontend && npm install --include=dev && npm run build) && (cd backend && npm install --include=dev && npm run build)"
  },
  "deploy": {
    "startCommand": "cd backend && node dist/db/migrate.js && (node dist/db/seedChoferes.js; node dist/server.js)",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 10,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
